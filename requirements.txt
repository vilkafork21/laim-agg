# Зависимостей нет: агрегатор работает на стандартной библиотеке
# (main.py и aggregator.py импортируют только logging и typing).
# Файл существует, потому что объявлен в libraryDependencies дескриптора:
# без него сборка контейнера падает на отсутствующем файле.
